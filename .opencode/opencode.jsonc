{
	// NOTE TO AGENTS: Use either folder based config OR file based config, not both.
	// Folder based config is recommended for most use cases.
	// File-based config is useful if sharing common config across multiple repositories or agents
	// See https://opencode.ai/docs/configuration for details.
	"$schema": "https://opencode.ai/config.json"
}
